components:
- name: github.com/vasu1124/introspect
  version: ${VERSION}
  provider: 
    name: actvirtual.com
  resources:
  - name: 'introspect-image'
    type: 'ociImage'
    version: ${VERSION}
    access:
      type: ociArtifact
      imageReference: ${OCI}/${ORG}/introspect:${VERSION}
  - name: 'introspect-blueprint'
    type: landscaper.gardener.cloud/blueprint
    input:
      type: dir
      path: "./blueprint/"
      compress: true
      mediaType: application/vnd.gardener.landscaper.blueprint.v1+tar+gzip
  - name: 'introspect-helm'
    type: helm
    version: ${VERSION}
    input:
      type: helm
      path: ../.gen/introspect/introspect-helm-${VERSION}.tgz
  sources:
  - name: 'introspect'
    type: 'git'
    version: ${VERSION}
    access:
      type: gitHub
      repoUrl: github.com/${ORG}/introspect
      ref: ${REF}
      commit: ${COMMIT}
